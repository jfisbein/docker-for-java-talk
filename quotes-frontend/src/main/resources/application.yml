micronaut:
  router:
    static-resources:
      images:
        paths:
          - "classpath:views"

  application:
    name: quotes-frontend

  server:
    port: 8081

  views:
    thymeleaf:
      suffix: ".html"
      enabled: true
      cacheable: false